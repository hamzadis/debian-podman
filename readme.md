Personal debian package repo for podman. Add it to apt sources like this:

    deb [trusted=yes] https://hamzadis.com/debian-podman/repo debian-podman main

Then update and install `podman5` and optionally `crun1`.

